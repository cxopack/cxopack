---
description: Cut a feature list into a 2-week shippable MVP with RICE scoring.
argument-hint: [the feature list or product area]
---

$ARGUMENTS

Run the **mvp-scope** skill. Force the hypothesis in one sentence: "If I ship X, then Y will do Z in N days, proven by metric M." RICE-score every feature, cut to top 5. Produce the "explicitly NOT in MVP" list with revisit triggers. Define kill criteria.

Refuse: lists longer than 6, "polish" as a feature, admin panels before 50 users, auth before signal.

Save to `docs/specs/mvp-<slug>.md`.
