---
description: Write an Architecture Decision Record (Michael Nygard format).
argument-hint: [the decision title]
---

$ARGUMENTS

Run the **adr** skill. Require: title, context, ≥2 alternatives, chosen decision, at least one *negative* consequence (force this), observable revisit trigger (not "if performance is an issue" — a specific threshold).

Auto-increment the ADR number from `docs/adr/`. Save as `docs/adr/NNN-<slug>.md`.
